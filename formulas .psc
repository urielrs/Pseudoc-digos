algoritmo formula 
    definir a , b , c , factorial como real
    escribir " ingrese los datos solicitados " ;
    leer a ;
    leer b ;
    leer c ; 
    factorial <- ( a b ) = a c / ( b c < ( a - b ) c ) ;
   escribir " factorial es = " ;
 finalgoritmo